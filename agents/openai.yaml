interface:
  display_name: "Better Word"
  short_description: "LaTeX report workflow with DOCX export"
  default_prompt: "Use $better-word to convert my school Word report template into reusable LaTeX and optional DOCX export."
